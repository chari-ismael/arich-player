{
  "name": "arich-fr",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node tools/apply-maintenance.mjs --off",
    "build:live": "vite build && node tools/apply-maintenance.mjs --off",
    "build:maintenance": "vite build && node tools/apply-maintenance.mjs --on",
    "preview": "vite preview"
  },
  "dependencies": {
    "lenis": "^1.2.3"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
